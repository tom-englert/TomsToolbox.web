// Tests for this adapter are browser specific, so they won't run with jest
// Tests are located in ../sample/src/spec/intl.adapter.spec.ts

describe('IntlAdapter', () => {
    it('is tested in app context', function () {
    });
});
